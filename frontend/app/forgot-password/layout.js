import '../../app/login/Auth.css';

export const metadata = {
  title: 'Forgot Password - RentNesto',
  description: 'Reset your RentNesto account password.',
};

export default function ForgotPasswordLayout({ children }) {
  return children;
}