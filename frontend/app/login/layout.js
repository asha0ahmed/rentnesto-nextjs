export const metadata = {
  title: 'Login - RentNesto',
  description: 'Login to your RentNesto account to find or list rental properties in Bangladesh.',
};

export default function LoginLayout({ children }) {
  return children;
}