export const metadata = {
  title: 'Browse Properties - RentNesto Bangladesh',
  description: 'Find apartments, hostels, sublets and rental properties across Bangladesh.',
};

export default function PropertiesLayout({ children }) {
  return children;
}